# --- Local dev (copy to .env.local) ---
VITE_API_URL=http://localhost:3000

# --- Production (Vercel Project → Environment Variables, Production) ---
# VITE_API_URL = https://your-api.onrender.com  (no trailing slash; set before deploy/build)
